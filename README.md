# Telegram Bot для управления каталогом товаров

Этот бот позволяет управлять каталогом товаров через Telegram Mini App с административной панелью.

## 🚀 Деплой на Scalingo

### 1. Подготовка
1. Создайте аккаунт на [scalingo.com](https://scalingo.com)
2. Установите Scalingo CLI: `npm install -g scalingo-cli`
3. Авторизуйтесь: `scalingo login`

### 2. Создание приложения
```bash
# Создайте новое приложение
scalingo create your-app-name --region osc-fr1

# Перейдите в директорию проекта
cd "C:\Users\htc27\OneDrive\Рабочий стол\BOT"
```

### 3. Настройка переменных окружения
В панели управления Scalingo (dashboard.scalingo.com) перейдите в ваше приложение → Environment:

```
TELEGRAM_BOT_TOKEN=ваш_токен_бота
MINI_APP_URL=https://your-app-name.scalingo.io
MANAGER_USERNAME=ваш_username_менеджера
ADMIN_USER_IDS=ваш_telegram_id
```

### 4. Деплой
```bash
# Добавьте remote репозиторий Scalingo
scalingo git-remote-add your-app-name

# Отправьте код
git push scalingo master
```

### 5. Проверка
- Бот должен запуститься автоматически
- Проверьте логи: `scalingo logs -a your-app-name`

### 📋 Архитектура
Приложение использует единственный веб-процесс, где:
- **Веб-сервер** (Flask + Gunicorn) обслуживает Mini App
- **Telegram бот** работает в фоновом потоке того же процесса
- **Каталог** обновляется в реальном времени через файловую систему

## 🤖 Настройка Telegram бота

### Получение токена бота
1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте токен и вставьте в переменную `TELEGRAM_BOT_TOKEN`

### Настройка Web App
1. В BotFather выберите вашего бота
2. Перейдите в настройки: `/setcommands` → добавьте команды
3. Настройте Web App: `/setmenubutton` → укажите URL вашего приложения

## 📱 Работа с каталогом

### Для администраторов
1. Напишите боту `/start`
2. Нажмите "Открыть приложение"
3. В приложении нажмите на черную кнопку ⚙️ (справа снизу)
4. В админ-панели вы можете:
   - **Редактировать JSON**: изменить структуру каталога
   - **Импорт/Экспорт**: загрузить/скачать catalog.json
   - **Сохранить на сервер**: применить изменения

### Структура каталога (catalog.json)
```json
{
  "categories": [
    {
      "id": "drinks",
      "title": "Напитки",
      "image": "https://example.com/image.jpg",
      "items": [
        {
          "id": "lemonade",
          "name": "Лимонад",
          "price": 250,
          "image": "https://example.com/item.jpg",
          "variants": ["Малина", "Груша", "Лимон"]
        }
      ]
    }
  ]
}
```

## 🔧 Локальная разработка

### Установка зависимостей
```bash
pip install -r requirements.txt
```

### Запуск бота локально
```bash
# Создайте .env файл на основе env.example
cp env.example .env

# Запустите бота
python -m bot.main
```

## 📊 Управление каталогом

### Добавление товаров
1. Откройте админ-панель в приложении
2. Нажмите "Загрузить текущий" чтобы увидеть текущий каталог
3. Отредактируйте JSON в текстовом поле
4. Нажмите "Применить" для предварительного просмотра
5. Нажмите "Сохранить на сервер" чтобы применить изменения

### Экспорт каталога
- В админ-панели нажмите "Экспорт JSON" - файл скачается на ваш компьютер

### Импорт каталога
- Нажмите "Импорт JSON" и выберите файл с компьютера

## 🛠 Технические детали

- **Фреймворк**: aiogram 3.10
- **Хостинг**: Scalingo (Python runtime)
- **База данных**: Файловая система (catalog.json)
- **Web App**: Ванильный JavaScript + Telegram Web App API

## 🔒 Безопасность
- Админ-доступ только по Telegram ID
- Каталог обновляется только администраторами
- Все изменения логируются

## 📝 Полезные команды

```bash
# Просмотр логов
scalingo logs -a your-app-name

# Перезапуск приложения
scalingo restart -a your-app-name

# Просмотр переменных окружения
scalingo env -a your-app-name
```

## 🆘 Troubleshooting

### Ошибка "Invalid return code from buildpack"
1. Убедитесь что есть файл `runtime.txt` с версией Python 3.11
2. Проверьте что есть файл `bot/__init__.py`
3. Проверьте что есть `serve_webapp.py` для веб-сервера
4. Убедитесь что `Procfile` содержит `web: gunicorn serve_webapp:application --bind 0.0.0.0:$PORT`
5. Проверьте логи сборки: `scalingo logs -a your-app-name`

### Бот не отвечает
1. Проверьте токен бота в переменных окружения
2. Проверьте логи: `scalingo logs -a your-app-name`
3. Убедитесь что переменные окружения установлены правильно

### Web App не загружается
1. Проверьте URL в настройках бота через @BotFather
2. Убедитесь что приложение запущено
3. Проверьте консоль браузера на ошибки

### Каталог не обновляется
1. Убедитесь что ваш Telegram ID указан в `ADMIN_USER_IDS`
2. Проверьте JSON на валидность (используйте jsonlint.com)
3. Попробуйте перезапустить приложение: `scalingo restart -a your-app-name`

### ImportError или ModuleNotFoundError
1. Убедитесь что структура папок правильная (bot/main.py, bot/__init__.py)
2. Проверьте что все зависимости указаны в requirements.txt
3. Проверьте логи на наличие ошибок импорта
